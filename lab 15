from math import dist

p = [(1, 2), (4, 5), (7, 8), (3, 1)]
m = 999
pair = ()
for i in range(len(p)):
    for j in range(i + 1, len(p)):
        d = dist(p[i], p[j])
        if d < m:
            m = d
            pair = (p[i], p[j])

print(pair, m)
